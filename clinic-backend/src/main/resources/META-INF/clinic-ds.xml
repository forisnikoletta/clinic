<?xml version="1.0" encoding="UTF-8"?>
<datasources>

    <!-- JTA datasource for EE deployment -->
    <datasource jndi-name="java:jboss/datasources/clinicDS"
                pool-name="clinicDS"
                enabled="true"
                use-java-context="true">

        <!-- a driver with this name must exist in WildFly's configuration -->
        <!-- more info here: https://synaptiklabs.com/posts/adding-the-mysql-jdbc-driver-into-wildfly/ -->
        <driver>h2</driver>

        <connection-url>
            jdbc:h2:mem:clinic;DB_CLOSE_DELAY=-1
        </connection-url>
        <security>
            <user-name>sa</user-name>
            <password></password>
        </security>

    </datasource>

</datasources>
